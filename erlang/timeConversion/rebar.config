{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps,
 [timeConversion]}.
{escript_main_app, timeConversion}.
{escript_name, timeConversion}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
